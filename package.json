{
  "name": "googlebooks-app",
  "version": "1.0.0",
  "description": "## Description",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hafsah1976/Book-Search-Engine.git"
  },
  "bugs": {
    "url": "https://github.com/hafsah1976/Book-Search-Engine/issues"
  },
  "homepage": "https://github.com/hafsah1976/Book-Search-Engine#readme",
  "engines": {
    "node": "16.x"
  }
}
